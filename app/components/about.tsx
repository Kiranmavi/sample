import React from 'react';

const About: React.FC = () => {
  return (
    <div id="about" className='w-full h-screen flex justify-center items-center'>About</div>
);
};

export default About;